<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.owner.cafetoolkit" >

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- To use the internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Function"
            android:label="@string/title_activity_function"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
        <activity
            android:name=".invList_activity"
            android:label="Inventory List"
            android:theme="@style/AppTheme.NoActionBar" >
        </activity>
        <activity android:name=".addDelete_user" >
        </activity>
        <activity android:name=".custTab_List" >
        </activity>
        <activity android:name=".custTab_Mod" >
        </activity>
        <activity android:name=".Functions" >
        </activity>
        <activity android:name=".invList" >
        </activity>
        <activity android:name=".invListActivity" >
        </activity>
        <activity android:name=".invMod" >
        </activity>
        <activity android:name=".shoppingList" >
        </activity>
        <activity android:name=".showUser" >
        </activity>
    </application>

</manifest>
